model Permit {
  id          String   @id @default(uuid())
  type        String   @unique @default("r") //r,rw,full
  description String?
  expiresAt   DateTime
  // owner    Employee[] @relation("EmplPermit")
}